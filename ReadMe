Please make sure to delete the sqlite3 Database in the project as well as the migrations in the /ProjectManagerTool/migrations folder. 

This is to ensure that you are not working with old and antiquated data that may crash the program.

When you want to run the program after completing this step, run python manage.py makemigrations followed by python manage.py migrate

Then, to start the program run "python manage.py runserver" and copy/paste the ip address given by the terminal window to open the web app in a browser of your choice. 
